spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/achievex
    username: achievex
    password: achievex123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration