services:
  postgres:
    image: postgres:16
    container_name: achievex-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: achievex
      POSTGRES_USER: achievex
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: